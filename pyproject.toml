[project]
name = "atcoder-ndpc"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "online-judge-tools>=11.5.1",
    "setuptools>=82.0.1",
]

[dependency-groups]
dev = [
    "isort>=8.0.1",
]
